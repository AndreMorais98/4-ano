doctype html
head
  meta(name='viewport' content='width=device-width, initial-scale=1.0')
  link(rel='stylesheet' href='/stylesheets/home.css')
  link(rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.1/css/all.css")
  link(rel="stylesheet" href="https://use.fontawesome.com/releases/v5.15.1/css/v4-shims.css")
.sidebar
  a(href='/home') Home
  a(href='/certificate') Create Certificates
  a.active(href='/list/certificate') List Certificates
  a(href='/crl') Import Certificate Revocation List
  a(href='/ocsp') OCSP Service
  a(href="/logout" ) 
    i(class="fas fa-sign-out-alt")
    |   Logout
.content
    h1 Certificate Content
    hr
    pre #{fst}

    hr
    h1 Installing this certificate in a server
    hr 
    p The following format can be used to install certificate into a server
    p Base 64 encoded certificate
    pre #{snd}

    p Base 64 encoded certificate with CA certificate chain in pkcs7 format
    pre #{trd}